<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.otams.group36">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OTAMS">

        <activity android:name=".activities.AdminHomeActivity" />
        <activity android:name=".activities.PendingRequestsActivity" />
        <activity android:name=".activities.RejectedRequestsActivity" />
        <activity android:name=".activities.ReviewUserActivity" />

        <activity
            android:name=".activities.TutorDashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.OTAMS" />
        <activity android:name=".activities.TutorCreateSlotActivity" />
        <activity android:name=".activities.TutorSlotsActivity" />
        <activity android:name=".activities.TutorRequestsActivity" />
        <activity android:name=".activities.TutorSessionsActivity" />

        <activity android:name=".activities.StudentRegistrationActivity" />
        <activity android:name=".activities.WelcomeActivity" />
        <activity android:name=".activities.RoleChooseActivity" />

        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/Theme.OTAMS" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OTAMS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
