# Project-wide Gradle settings.
# NOTE: Android Studio's "Gradle JDK" setting will be used.
# Do NOT hardcode org.gradle.java.home here when using the IDE-managed JDK.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# JVM args for Gradle daemon (adjust heap if needed)
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX and R class namespacing
android.useAndroidX=true
android.nonTransitiveRClass=true

# Kotlin code style
kotlin.code.style=official

# REMOVE any org.gradle.java.home line (platform-specific and causes errors on Windows)
# org.gradle.java.home=...   <-- delete this line if present

